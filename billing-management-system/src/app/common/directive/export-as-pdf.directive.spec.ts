import { ExportAsPdfDirective } from './export-as-pdf.directive';

describe('ExportAsPdfDirective', () => {
  it('should create an instance', () => {
    const directive = new ExportAsPdfDirective();
    expect(directive).toBeTruthy();
  });
});
